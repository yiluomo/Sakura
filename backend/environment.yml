name: Sakura
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
    - fastapi
    - uvicorn
    - httpx
    # 数据库相关
    - sqlalchemy>=2.0
    - aiomysql  # MySQL异步驱动
    - pymysql   # MySQL同步驱动（用于alembic迁移）
    - cryptography  # aiomysql依赖
    # 可选：数据验证
    - pydantic>=2.0
