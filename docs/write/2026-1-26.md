# 2026-1-26
不同的模型调用可以放在不同的模型文件写函数，里然后统一在ataper里调用控制

成功接入qwen:8b本地模型，但对话时风扇剧烈旋转，明显难以支撑，考虑下调精度或继续使用deepseek在线api

完成了记忆功能的初步增加。后续还需增加短期记忆的总结保存，遗忘机制。和长期记忆的更灵活更新机制。不过当下已经可以实现初步对话。后端的日志文件似乎没有日志保存。

长期记忆的关键词自动更新有重大bug。不可用